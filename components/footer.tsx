import CreateExpenses from './createExpenses';

const Footer = () => {
  return (
    <footer className="fixed bottom-0 left-0 w-full flex justify-center items-center bg-gray-700 p-1 rounded-md">
        <CreateExpenses/>
    </footer>
  );
};

export default Footer;